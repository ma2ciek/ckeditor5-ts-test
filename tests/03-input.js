/** @type {import('./utils').Point} */
let point;

/** @type {import('./interface').Point} */
const point2 = { x: 0, y: 1 };

function x() {
	/** @type {import('./interface').Point} */
	const point3 = { x: 0, y: 1 };
}

export {
	point,
	point2
};
